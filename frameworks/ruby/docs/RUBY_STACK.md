# Ruby Stack

## Commands

```bash
bundle install
bundle exec rspec
bundle exec rails test
bundle exec rubocop
bundle exec rails server
```

## Structure (typical)

```
app/
config/
spec/ or test/
Gemfile
```

## Validation before done

`bundle exec rspec` (or rails test) and rubocop if configured.
