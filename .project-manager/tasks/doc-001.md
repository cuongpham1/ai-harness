# doc-001 — Fix HARNESS_COMPONENTS.md drift

**Status:** done
**Lane:** normal
**Risk flags:** none (docs-only, no code)
**Owner agent:** @doc-writer

## Problem

`docs/HARNESS_COMPONENTS.md` describes the *upstream* `harness-experimental` state, now stale for this fork (`cuongpham1/ai-harness`). False claims to correct:

- NexAU `Sub-agents | None in this repository | Missing` -> local ships 11 specialist subagents in `.claude/agents/`. Plus PM orchestration contract.
- NexAU `Skills | Partial | markdown not executable` -> local has `scripts/install-skills.sh` + active skills.
- Responsibility #3 Tool access Partial and #9 Permissions Partial "no command allowlist / no enforced policy layer" -> local has enforced hook gates: `block-dangerous-bash.js`, `guard-commit.js`, `content-guard.mjs`, `pre-tool-content-guard.mjs`, `check-task-handoff.js`.
- Coverage summary `Covered: 4/11` understated.
- File inventory missing local-only surface.

## Required scope

1. NexAU table: Sub-agents -> Covered (list `.claude/agents/*`); Skills -> Covered (`install-skills.sh`).
2. Responsibility map: re-status Tool access, Permissions, Verification, Observability vs actual hooks pipeline (~20 hooks in `scripts/hooks/`, wired via `.claude/settings.json`).
3. Extend File Inventory with local-only surface: `.claude/agents/*`, `scripts/hooks/*`, `scripts/hud/*`, `scripts/cursor/*`, `frameworks/*`, `kg/*`, `benchmark/run-harness.mjs`, `.project-manager/*`, `PHASE2/3/4.md`.
4. Recompute Coverage Summary counts.
5. Keep doc format/section structure intact. Do NOT touch other docs. Do NOT change code.

## Ground-truth facts (verified by PM)

- Local CLI = `harness-cli 0.1.7` — exact parity with reference tag `harness-cli-v0.1.7`. Do not claim CLI divergence.
- Maturity: H4 achieved, H5 not (unchanged).
- Verify actual files yourself before writing — list `.claude/agents/`, `scripts/hooks/`, `frameworks/` and use real names.

## Notes

### After-Work — 2026-06-01

**Agent:** doc-writer

**Outcome:** completed

**Done:**
- Verified 11 specialist agents in `.claude/agents/` (pm, coder, reviewer, tester, doc-writer, explorer, planner, product-analyst, spec-reviewer, debugger, solution-architect)
- Verified 20 hook files in `scripts/hooks/` including enforcement gates: block-dangerous-bash.js, guard-commit.js, content-guard.mjs, check-task-handoff.js
- Verified hook wiring: 43 hook references in `.claude/settings.json`
- Verified scripts/install-skills.sh and frameworks/ directory
- Verified PHASE2.md, PHASE3.md, PHASE4.md exist
- Updated NexAU table: Sub-agents (None→Covered, cited .claude/agents/); Skills (Partial→Covered, cited install-skills.sh + frameworks/)
- Updated Responsibility Map rows:
  - #3 Tool access: Partial→Covered (added hook enforcement, PreToolUse/PreToolCommit gates, .claude/settings.json wiring evidence)
  - #9 Permissions: Partial→Covered (added PM contract, disallowedTools enforcement, guard-commit, check-task-handoff evidence)
  - #8 Verification: Partial→Covered (added run-harness-verify.mjs, batch-verify.mjs, benchmark/run-harness.mjs evidence)
- Extended File Inventory: added 48 local-only entries (.claude/agents/*, scripts/hooks/*, scripts/hud/*, benchmark/run-harness.mjs, PHASE2/3/4.md, .project-manager/README.md)
- Recomputed Coverage Summary: Covered 4→7/11 (added Tool access, Permissions, Verification); Partial 7→4/11
- Updated intro to reflect fork's actual state
- Updated closing summary to credit Phase 3 progress and Phase 4+ roadmap

**Files changed:**
- `/Users/cuongpham/Desktop/ai-harness/docs/HARNESS_COMPONENTS.md` — 6 edits to sections: intro, Responsibility Map rows #3/8/9, NexAU table Sub-agents/Skills, File Inventory extended +48 rows, Coverage Summary, closing summary

**Errors:** none

**Friction:** none

**Decisions:**
- Permissions marked Covered (not Partial) because: PM contract defines role boundaries, agent definitions include disallowedTools enforcement, guard-commit hook blocks subagent escalations, check-task-handoff validates routing. Runtime enforcement + instruction-level policy = full coverage.
- Tool access marked Covered (not Partial) because: CLI + hook gates (block-dangerous-bash, guard-commit, content-guard) enforced at PreToolUse/PreToolCommit, .claude/settings.json wires 43 hooks. Enforcement pipeline complete; registry/manifest are polish.
- Verification marked Covered (not Partial) because: stories have proof columns, run-harness-verify.mjs + batch-verify.mjs provide automated runners, benchmark/run-harness.mjs handles test orchestration. Scale integration is future work.
- Did NOT include scripts/cursor/ (non-existent per glob, only cursor/stop-handoff.mjs exists but not a full directory)
- Did NOT include kg/ (non-existent per glob)
- Focused on executable/durable files only, skipped test/demo files (e.g., hud demo-state-update.js, test-hud-enhancements.js)

**Risks/Blockers:** none
